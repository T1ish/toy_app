<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<p>
	<strong>First post</strong>
	<!-- The line below won't work in production because there is no data in the databased. -->
	<!-- <%= @user.microposts.first.content %> -->
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
